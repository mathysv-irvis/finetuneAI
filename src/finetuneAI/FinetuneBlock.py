from transformers.modeling_outputs import ModelOutput
from datasets import Dataset

class FinetuneBlock:

    def __init__(self):
        return

    def train(self, dataset:Dataset, *args, **kwargs) -> None:
        return

    def merge(self, **kwargs) -> ModelOutput:
        return
